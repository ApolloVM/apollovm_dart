name: apollovm_lsp_benchmark
description: Performance harness for the ApolloVM language server.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  apollovm_lsp:
    path: ../apollovm_lsp
