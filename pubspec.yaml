name: apollovm
description: >-
  Portable multi-language VM: parse, run and translate Dart, Java, Kotlin, Go,
  C#, JavaScript, TypeScript, Lua and Python — with on-the-fly Wasm compilation
  and MCP/LSP servers.
version: 2.25.0
repository: https://github.com/ApolloVM/apollovm_dart
issue_tracker: https://github.com/ApolloVM/apollovm_dart/issues

topics:
  - vm
  - dart
  - java
  - compiler
  - translator

screenshots:
  - description: 'ApolloVM Logo'
    path: example/apollovm-logo.png

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  swiss_knife: ^3.3.14
  async_extension: ^1.2.22
  data_serializer: ^1.2.2
  petitparser: ^7.0.2
  collection: ^1.19.1
  args: ^2.7.0
  wasm_interop: ^2.0.1
  path: ^1.9.1
  web: ^1.1.1
  dart_mcp: ^0.5.2
  stream_channel: ^2.1.4
  # Optional Dart package importer (`PubDevProvider`) only — all web-safe.
  # The default `PackageConfigProvider` uses none of these.
  http: ^1.6.0
  archive: ^4.0.9
  pub_semver: ^2.2.0
  yaml: ^3.1.3

dev_dependencies:
  # The native (VM) Wasm engine lives outside this package (see `apollovm_wasm`),
  # so the Wasm execution tests pull it back in here.
  apollovm_wasm:
    path: apollovm_wasm
  lints: ^6.1.0
  dependency_validator: ^5.0.5
  test: ^1.31.1
  pubspec: ^2.3.0
  xml: ^6.6.1
  build_web_compilers: ^4.8.0
  build_runner: ^2.15.0

executables:
  apollovm:
